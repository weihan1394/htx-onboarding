services:
  valkey:
    image: valkey/valkey:9-alpine
    container_name: valkey
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    networks:
      - htx-network
    restart: unless-stopped

networks:
  htx-network:
    external: true
